name: 'Deploy Action'
description: 'Simple deployment action'
author: 'Test Team'

branding:
  icon: 'upload'
  color: 'green'

runs:
  using: 'composite'
  steps:
    - name: Deploy Application
      shell: bash
      run: |
        echo "Application deployed"